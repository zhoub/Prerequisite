#
# Prerequisite
#

CMAKE_MINIMUM_REQUIRED(VERSION 3.7)

PROJECT(Prerequisite)

INCLUDE(ExternalProject)

# Generators
SET(GENERATOR_VS2012_WIN64 "Visual Studio 11 2012 Win64")
SET(GENERATOR_VS2015_WIN64 "Visual Studio 14 2015 Win64")

SET(GENERATOR_MAKEFILE "Sublime Text 2 - Unix Makefiles")

SET(GENERATOR_XCODE "Xcode")


# Compilers
#
SET(COMPILER_CXX_STANDARD_11 "CPP11")

# Platforms
#
SET(PLATFORM_WIN64_VS2010 "VS2010-Win64")
SET(PLATFORM_WIN64_VS2012 "VS2012-Win64")
SET(PLATFORM_WIN64_VS2015 "VS2015-Win64")

# Linux
SET(PLATFORM_LINUX_GCC412       "Linux-GCC412")
SET(PLATFORM_LINUX_GCC447       "Linux-GCC447")
SET(PLATFORM_LINUX_GCC482       "Linux-GCC482")
SET(PLATFORM_LINUX_GCC482_CPP11 "Linux-GCC482-${COMPILER_CXX_STANDARD_11}")

# OSX
SET(PLATFORM_OSX_GCC         "OSX-GCC")
SET(PLATFORM_OSX_CLANG       "OSX-Clang")
SET(PLATFORM_OSX_CLANG_CPP11 "OSX-Clang-${COMPILER_CXX_STANDARD_11}")

# OSX-specific
#
SET(OSX_TARGET_SNOW_LEOPARD  "10.6")
SET(OSX_TARGET_MOUNTAIN_LION "10.8")
SET(OSX_TARGET_MAVERICKS     "10.9")
SET(OSX_TARGET_YOSEMITE      "10.10")
SET(OSX_TARGET_EL_CAPITAN    "10.11")
SET(OSX_TARGET_SIERRA        "10.12")

# Modules
#
LIST(APPEND CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR})

INCLUDE(Boost)
INCLUDE(ZLib)
INCLUDE(OpenEXR)
INCLUDE(GLEW)
INCLUDE(DoubleConversion)
